/**
 * returns set of an array
 * @param {Array<any>} array - list of any type
 *
 * @returns {Set} set of a given array
 */
export default function setFromArray(array) {
  return (new Set(array));
}
